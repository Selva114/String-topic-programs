//Check Apple--->count how many p and l 

public class LetterCount
{
public static void main(String args[])
{
String s="Apple";

int count=0;
int count1=0;

    for(int i=0;i<s.length();i=i+1) 
    {
        if(s.charAt(i)=='p')
        {
        count=count+1;
        }
        if(s.charAt(i)=='l')
        {
        count1=count1+1;
        }
    }
System.out.println("In given word p is present "+count + "times and l is present " +count1 +"times.");

}
}
